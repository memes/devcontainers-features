---
name: "CI - Test Features"
# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      features: ${{ steps.filter.outputs.changes }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            buf:
              - 'src/buf/install.sh'
              - 'test/buf/**'
            flux:
              - 'src/flux/install.sh'
              - 'test/flux/**'
            gcrane:
              - 'src/gcrane/install.sh'
              - 'test/gcrane/**'
            grpcurl:
              - 'src/grpcurl/install.sh'
              - 'test/grpcurl/**'
            google-cloud-cli:
              - 'src/google-cloud-cli/install.sh'
              - 'test/google-cloud-cli/**'
            goreleaser:
              - 'src/goreleaser/install.sh'
              - 'test/goreleaser/**'
            hashicorp:
              - 'src/hashicorp/install.sh'
              - 'test/hashicorp/**'
            hadolint:
              - 'src/hadolint/install.sh'
              - 'test/hadolint/**'
            talisman:
              - 'src/talisman/install.sh'
              - 'test/talisman/**'
            terraform-docs:
              - 'src/terraform-docs/install.sh'
              - 'test/terraform-docs/**'
            terragrunt:
              - 'src/terragrunt/install.sh'
              - 'test/terragrunt/**'
            tflint:
              - 'src/tflint/install.sh'
              - 'test/tflint/**'
            vesctl:
              - 'src/vesctl/install.sh'
              - 'test/vesctl/**'

  test-autogenerated:
    needs: changes
    if: ${{ needs.changes.outputs.features != '[]' }}
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: read
    strategy:
      matrix:
        features: ${{ fromJSON(needs.changes.outputs.features) }}
        baseImage:
          - mcr.microsoft.com/devcontainers/base:alpine
          - mcr.microsoft.com/devcontainers/base:debian
          - mcr.microsoft.com/devcontainers/base:ubuntu
          - registry.fedoraproject.org/fedora:latest
          - registry.access.redhat.com/ubi9/ubi:latest
    steps:
      - uses: actions/checkout@v7

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' against '${{ matrix.baseImage }}'"
        run: devcontainer features test --skip-scenarios -f ${{ matrix.features }} -i ${{ matrix.baseImage }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  test-scenarios:
    needs: changes
    if: ${{ needs.changes.outputs.features != '[]' }}
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: read
    strategy:
      matrix:
        features: ${{ fromJSON(needs.changes.outputs.features) }}
    steps:
      - uses: actions/checkout@v7

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' scenarios"
        run: devcontainer features test -f ${{ matrix.features }} --skip-autogenerated --skip-duplicated
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  test-global:
    needs: changes
    if: ${{ needs.changes.outputs.features != '[]' }}
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Testing global scenarios"
        run: devcontainer features test --global-scenarios-only
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
