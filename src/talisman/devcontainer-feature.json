{
    "id": "talisman",
    "name": "talisman",
    "version": "1.1.0",
    "description": "Installs Talisman from GitHub release and adds it to the global PATH.",
    "documentationURL": "https://github.com/memes/devcontainers-features/tree/main/src/talisman",
    "licenseURL": "https://github.com/memes/devcontainers-features/tree/main/LICENSE",
    "keywords": [
        "talisman",
        "precommit",
        "pre-commit",
        "creds",
        "credentials"
    ],
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest"
            ],
            "default": "latest",
            "description": "The specific version to install; default value is 'latest' which will install the most recent available GitHub release. NOTE: If a version is provided and a matching GitHub Release can not be found the feature will fail."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
